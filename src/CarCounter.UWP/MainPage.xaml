<Page
    x:Class="CarCounter.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CarCounter.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Canvas">
        <Grid x:Name="MainView">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <RelativePanel Grid.Column="0" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                    <Button
                        x:Name="BtnStart"
                        Content="Start"
                        FontSize="32*"
                        Padding="24 0 24 0"/>
                </RelativePanel>

                <RelativePanel Grid.Column="1" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                    <Button
                        x:Name="BtnStop"
                        Content="Stop"
                        FontSize="32*"
                        Padding="24 0 24 0"/>
                </RelativePanel>

                <RelativePanel Grid.Column="2" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                    <ComboBox x:Name="CctvIp" 
                              Header="CCTV IP" 
                              PlaceholderText="Select CCTV IP here..."
                              FontSize="32*"
                              Margin="18 0 18 0">
                        <x:String>test 1</x:String>
                        <x:String>test 2</x:String>
                        <x:String>test 3</x:String>
                    </ComboBox>
                </RelativePanel>

                <StackPanel Grid.Column="3" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Stretch">

                    <Grid VerticalAlignment="Center"
                          HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" 
                                Text="Sync to Cloud"
                                FontSize="32*"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                        <ToggleSwitch Grid.Row="1"
                                FontSize="28*"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                x:Name="SyncSwitch"
                                IsOn="False"/>
                    </Grid>
                </StackPanel>

                <!-- just on dev -->
                <RelativePanel x:Name="" Grid.Column="4" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                    <Button
                        x:Name="BtnGetFromFile"
                        Content="From File"
                        FontSize="32*"
                        Padding="24 0 24 0"/>
                </RelativePanel>
            </Grid>
            <Grid Grid.Row="1"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">
                <Image x:Name="EyeOne"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Source="/Assets/sauron_eye.jpg"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
